package io.cortavyn.model.api;

/** A portable capability exposed by a model profile. */
public enum ModelCapability {
    STREAMING, TOOLS, STRUCTURED_OUTPUT, IMAGE_INPUT, AUDIO_INPUT, DOCUMENT_INPUT, REASONING
}
